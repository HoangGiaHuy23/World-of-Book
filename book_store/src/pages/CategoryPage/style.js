import styled from 'styled-components'
import { Row, Col } from 'antd'

export const WrapperContent = styled(Row)`
    gap: 10px;
`

export const WrapperBox = styled(Col)`
    background-color: #FFF;
    border-radius: 10px; 
    padding: 10px 10px;
    height: fit-content;
`

export const WrapperList = styled(Row)`
    display: flex; 
    gap: 14px; 
    padding: 5px 5px;
`